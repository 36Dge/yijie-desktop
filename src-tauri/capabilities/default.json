{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal local capability for the main Yijie window",
  "windows": ["main"],
  "platforms": ["macOS"],
  "permissions": ["core:app:default"]
}
