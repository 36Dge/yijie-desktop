# 组件配色与状态矩阵

## 文档状态

- 状态：Accepted
- 版本：2.1.0
- 最后更新：2026-09-06
- 依据：用户已确认 2026-09-06 组件配色方案并授权执行正式规范与应用同步。
- 适用范围：所有新页面、现有页面、共享控件、文档站及迁移参考。
- 保留范围：字体、字号、间距、圆角、布局和业务状态行为不变；已批准 Logo 资产保持不变。

本矩阵是组件颜色与状态组合的权威来源，与 [颜色 Tokens](../02-tokens/02-color-tokens.md) 共同使用。[已批准评审依据](../08-governance/08-component-color-convergence.md) 保留原候选与离线证据的冻结位置；运行代码不能导入候选或 exports。

## 1. 阅读方式：每个状态如何落到明暗配色

本矩阵使用角色缩写，避免每个单元格重复十六进制色值。下表为每个角色同时定义亮色与暗色；后续配置写成 `前景 / 背景 / 边框`。除明确写出 F 或 OSh 的状态外，**焦点为无、阴影为 none**。所有边框默认 1 px；“透明”表示使用最近的结构表面，不会注入灰底。

“＝默认”表示前景、背景、边框、焦点和阴影五项全部继承该组件 default；“＋F”只叠加中性焦点，不覆盖选中、错误边框或背景；“＋Busy”保留视觉并增加同前景色进度图标与可访问状态。`N/A` 表示该非交互对象没有该状态，不能为了填满矩阵新增交互。

### 颜色角色与精确值

| 缩写 / 角色 | 亮色 | 暗色 | 使用范围 |
|---|---|---|---|
| P / 主文字、普通操作图标 | `#25282B` | `#F5F7FA` | 标题、按钮标签、导航、工具图标 |
| T / 正文 | `#25282B` | `#E6E9ED` | 长段落、输入内容、卡片说明中的主要信息 |
| S / 次文字 | `#51565D` | `#C2C7CE` | 补充说明、次级标签；不能代替全部正文 |
| M / 元信息 | `#6F757D` | `#969EA8` | 时间、单位、数量辅助、占位说明 |
| D / 禁用前景 | `#A5ABB2` | `#69717B` | 确实不能操作的控件；不用于错误原因、只读值或空态解释 |
| N / App、页面、导航表面 | `#FFFFFF` | `#191C20` | 大面积结构空间 |
| C / 普通卡片、输入表面 | `#FFFFFF` | `#25282B` | 内容容器 |
| O / 浮层表面 | `#FFFFFF` | `#2E3237` | 下拉、弹窗、弹出菜单 |
| H / 小控件 hover 底 | `#F7F8F9` | `#30363D` | 仅未选中小控件、可操作列表行局部反馈 |
| A / 小控件 pressed 底 | `#ECEEF0` | `#414850` | 仅按压反馈；不作结构底色 |
| DB / 禁用控件底 | `#F7F8F9` | `#30363D` | 仅小型不可用控件 |
| B / 普通边框 | `#E2E5E8` | `#414850` | 卡片、次按钮、静态边界 |
| BC / 输入与选择控件边界 | `#8C939B` | `#6A727C` | 输入、checkbox/radio 等需要清楚识别的轮廓 |
| BH / 控件 hover 边界 | `#60666E` | `#969EA8` | 可操作边界反馈 |
| L / 品牌青柠填充 | `#C3F35B` | `#C3F35B` | 主操作、明确选中标记 |
| LH / 青柠 hover 填充 | `#D0F780` | `#D0F780` | 实色青柠控件 hover |
| LA / 青柠 pressed 填充 | `#B1E343` | `#B1E343` | 实色青柠控件 pressed |
| ON / 青柠上前景 | `#25282B` | `#25282B` | 青柠上的文字、勾选、圆点和图标 |
| TR / 未选中 Switch 轨道 | `#B8BEC5` | `#B8BEC5` | 小型开关局部，不能用于背景容器 |
| F / 中性焦点色 | `#25282B` | `#F5F7FA` | 2 px 清晰轮廓、无模糊、无 glow |
| E / 错误边框语义 | 既有 `#DC2626` | 既有 `#DC2626` | 局部错误边框；配可读正文与明确错误消息 |
| EI / 错误文字与图标 | `#B91C1C` | `#FCA5A5` | 错误标题、小标签、状态图标；长说明保持 T |
| SI / 成功文字与图标 | `#166534` | `#86EFAC` | 真实成功状态的小标签、趋势状态文字 |
| WI / 警告文字与图标 | `#92400E` | `#FCD34D` | 真实 warning 状态 |
| II / 信息文字与图标 | `#1D4ED8` | `#93C5FD` | 真实 info 状态 |
| OSh / 浮层轻投影 | `0 6px 20px rgba(37,40,43,.10)` | `0 6px 20px rgba(0,0,0,.28)` | 仅真实浮层；普通卡片和输入为 none |

SI/WI/EI/II 是 2.1.0 新增的可读语义前景，分别映射到 `--yj-color-semantic-success-ink` / `warning-ink` / `error-ink` / `info-ink`。它们不更改原状态主色、状态含义或图表色；Alert 长正文仍使用 T，不整段继承状态色。

边框颜色服务两种不同目的：B 组织空间，BC 提供控件识别。装饰卡片不需要通过加深全部边框抢占注意，但输入边界不能弱到无法识别。F 是 2 px 无模糊的环或 outline；实现即使用零模糊 `box-shadow` 绘制也不属于投影，不能额外叠加 card shadow 或 glow。

## 2. 文字与图标的角色矩阵

| 对象 | 默认前景 / 背景 / 边框 | 其他状态 |
|---|---|---|
| 页面/卡片标题、指标主值 | `P / 透明 / 无` | hover、pressed、selected、focus、readonly、disabled、loading、error 均 N/A；容器状态不把标题一并变灰/变绿 |
| 正文、输入值、关键说明 | `T / 透明 / 无` | 静态正文状态均 N/A；输入正文按输入矩阵；loading/error 的解释仍可读 |
| 辅助段落、次标签 | `S / 透明 / 无` | 静态状态均 N/A；不能因页面 loading/permission denied 降为 D |
| 元信息、时间、单位、placeholder | `M / 透明 / 无` | placeholder 不能承载唯一标签；禁用输入的 placeholder 才允许 D |
| 普通工具图标 | `P / 透明 / 无` | 装饰图标状态 N/A；交互图标按图标按钮矩阵，不能把 SVG 固定染绿 |
| 辅助/元信息图标 | `M / 透明 / 无` | 仅辅助；可点击时使用 P 并提供可访问名称 |
| 已禁用图标/标签 | `D / DB 或透明 / B 或无` | 不再响应 hover/pressed；禁止对整个内容区域统一 opacity 降低 |
| 状态图标和状态文案 | 图标使用独立语义色，文案 `T` 或 `S` | 错误、成功、风险需文字一起表达；不能让普通正文继承状态色 |

文字阅读层级通过角色与既有字重实现。不可把普通描述、权限说明、错误原因或只读值当作“次要到可以忽略”的灰字，也不可把深绿文字视为专业感来源。普通链接使用 P，配下划线或可见链接语义；其 focus 使用 F，不用浅青柠小字。

## 3. 完整组件状态矩阵

### 3.1 按钮、图标按钮与文字操作

每个单元格仍为 `前景 / 背景 / 边框`；没有 F 或 OSh 时焦点与阴影均为无。

| 状态 | 主按钮 | 次按钮（白底描边） | 文字/图标按钮 |
|---|---|---|---|
| default | `ON / L / BC` | `P / C / B` | `P / 透明 / 无` |
| hover | `ON / LH / BH` | `P / H / BH` | `P / H / 无`；文字链接可维持透明并下划线 |
| pressed | `ON / LA / BH` | `P / A / BH` | `P / A / 无` |
| selected | N/A：执行动作不等于选择状态 | 切换类才使用 `ON / L / ON`；普通动作 N/A | 切换图标 `ON / L / ON`；普通动作 N/A |
| focus | 当前 default/hover/selected `＋F` | 当前状态 `＋F` | 当前状态 `＋F` |
| readonly | N/A：动作按钮不以 readonly 模拟 disabled | 同左 | 同左；复制只读值仍是正常可操作按钮 |
| disabled | `D / DB / B`，不减整控件 opacity | `D / DB / B` | `D / 透明 / 无` |
| loading | 保留动作前景/填充 `＋Busy`，spinner 使用 ON | 保留 `P / C / B ＋Busy` | 保留 P，spinner 不单独染绿 |
| error | 动作按钮不自动变红；错误信息独立显示，允许重试的按钮保持原层级 | 同左 | 同左 |

有描边的 ghost 品牌按钮采用 `P / 透明 / BC`，hover / pressed 边界为 BH，focus 为 F；无描边 text 与图标按钮按表中“文字/图标按钮”列。

普通 `secondary` 产品语义应使用白底次按钮配色；Naive UI 的同名 prop 不是允许默认灰底的理由。`secondary/tertiary/quaternary + type="primary"` 的组合会绕过部分文字主题键，评审将它们作为反例展示，不作为推荐用法。实际实现应使用经过主题验证的实心 primary、普通/secondary 中性按钮或共享封装，不能在业务页面补一串临时色值。

危险按钮继续走既有 danger/error 语义配色与确认流程；“接口返回错误”不能把所有主按钮永久染红。品牌主按钮中 disabled 是能力不可用，而 loading 是动作进行中：仅为阻止重复提交而设置 disabled 的 loading 按钮应保持原前景、进度与 busy 表达，不伪装为功能被禁用。

### 3.2 输入框、搜索框与 Chat Composer

2026-09-07 用户调整：新建任务与继续对话的 Chat Composer 聚焦、拖放态采用单层 1 px 品牌青柠边框（`--yj-border-width` / `--yj-color-brand-primary`），不叠加外圈或阴影。此项覆盖本矩阵通用 F 规则对 Composer 外框的约束；内部按钮仍保留各自键盘焦点。新建任务标题上方移除“本地 AI 工作区”。

| 状态 | 内容前景 / 背景 / 边框 | 焦点 | 阴影及说明 |
|---|---|---|---|
| default | `T / C / BC`；placeholder=M，label=P | 无 | none |
| hover | `T / C / BH` | 无 | none；不改变输入填充 |
| pressed | `T / C / BH` | 若获得焦点使用 F | none；按压不是长期状态 |
| selected | 仅已选文字 `ON / L`：两主题文本选区均为青柠底、石墨字；其余内容仍 `T / C` | 原焦点可保留 | none；不把整个输入改为青柠底 |
| focus | 普通 Input：`T / C / F`；Composer：`T / C / L` | 普通 Input：2 px F；Composer：1 px 青柠边框 | 无投影 / glow；Composer 不叠加外圈 |
| readonly | `T / C / BC`；保留可选择/复制的值及“只读”说明 | 键盘可达时仍有 F | none；不使用 D、DB 或禁用光标伪装只读 |
| disabled | `D / DB / B` | 原生 disabled 为 N/A | none；不得拿禁用样式承载权限原因正文 |
| loading | `T / C / BC`；spinner 使用 P，错误/警告 spinner 分别使用 EI/WI；明确 busy 信息 | 已聚焦则 F | none；保持现有内容，不把正文清空成不可读骨架 |
| error | `T / C / E`；消息正文=T/S，错误标记=EI | 有焦点时仍叠 F | none；E 边框不被 F 覆盖，F 也不被 error 清除 |

输入的 readonly 与 disabled 是不同语义。只读值有信息价值，仍使用正常文字对比；disabled 只表示无法使用的控件。审批的“只读 · 禁止写入”是正常权限说明，不是禁用文字。文本选区使用 `text-selection-bg / text-selection-ink`，只改变绘制色，不改变原生选词、键盘选择、复制和输入法行为；它与输入框焦点可以并存。Composer 拖放态采用单层 1 px 青柠边框与拖放说明；附件真实错误、警告仍使用既有语义标记，不叠外环。

### 3.3 主导航、标签页与筛选

| 状态 | 主导航项 | 小型筛选 / 可选芯片 | 内容标签页（线形 Tabs） |
|---|---|---|---|
| default | `P / N / 无` | `P / C / B` | `S / C / 无` |
| hover | `P / H / 无`，仅该项局部 | `P / H / BH` | `P / C / 无`，可用中性下划线 |
| pressed | `P / A / 无`，仅该项局部 | `P / A / BH` | `P / C / 无` |
| selected | `P / N / 无` + **内侧 3 px L 标记** | `ON / L / ON` | `P / C / 无` + 中性可见下划线 |
| selected + hover/pressed | hover 用 H、pressed 用 A，均限当前小项并保留 3 px L | hover=`ON/LH/ON`；pressed=`ON/LA/ON` | 维持选中下划线与可读 P |
| focus | 当前状态 `＋F`；不抹掉 L 标记 | 当前状态 `＋F` | 当前状态 `＋F` |
| readonly | N/A：导航行为不以 readonly 表达 | 展示当前值时使用非交互文本/标签，保留正常 P；不伪造可切换项 | 不可切换但需展示当前分类时按静态标题，不称 disabled 内容 |
| disabled | `D / N / 无`，无 hover/pressed | `D / DB / B`，已选值可保留标记但不使用亮色填充 | `D / C / 无`；当前内容不因此变灰 |
| loading | 导航本身不染色；子项 loading 由内容说明；正在导航可同色 busy | 当前状态 `＋Busy` | 当前状态 `＋Busy` |
| error | 当前项颜色不替换；错误放内容区/可访问描述 | 错误信息独立，保留筛选值 | 错误由面板内显示，tab 的 selected 仍保留 |

3 px 导航标记表达位置，2 px F 表达键盘焦点，二者必须共存。选择导航只改变路由状态；颜色规范不改变权限过滤、disabled、当前路由或导航顺序。可见 selected 文案与 `aria-current` / `aria-selected` 仍是状态依据，不只依靠青柠。

### 3.4 Checkbox、Radio、Switch、Select

| 状态 | Checkbox / Radio | Switch | Select / 下拉选项 |
|---|---|---|---|
| default（未选中） | 标记无；label=T；`C / BC` | 轨道 TR，滑块 ON，label=T | 输入 `T / C / BC`；选项 `T / O / 无` |
| hover | label=T，控件 `C / BH` | 维持 TR/ON，不出现绿色 glow | 输入 `T/C/BH`；未选选项 `T/H/无` |
| pressed | 维持当前选值，按既有交互提交；不预先伪造 checked | 维持当前选值，提交后更新；无投影 | 输入保持 C；选项 `T/A/无` |
| selected | 填充 L，勾/圆点 ON，边界 ON；label=T | 轨道 L，滑块 ON，轨道文字 ON | 输入保持 `T/C/BC`；已选选项 `P/O/无` + P 勾；不用软绿底 |
| selected + hover | 保留 checked/ON；hover 不覆盖勾 | 保留 L/ON | 已选项可用局部 H 反馈；P 勾和选中语义保留 |
| focus | 当前配色 `＋F` | 当前配色 `＋F` | 输入/选项当前配色 `＋F`；浮层仍用 OSh |
| readonly | 不提供伪造原生 readonly；用静态选值呈现，正常 label T，保留可读选值 | 静态“开启/关闭”文本，正常 T，不渲染假可切换控件 | `T/C/BC`；可复制，不打开选择菜单，正常只读说明 |
| disabled | label/勾=D，局部 DB/B；既有 checked 值不丢 | 轨道 DB、滑块 D、状态文字 D，保留滑块位置；无 hover | `D/DB/B`；不可选项 D/O；原因说明仍 T/S |
| loading | 当前选值和正常前景 `＋Busy`，不闪回未选中 | 状态不提前切换；busy 保持正常读值 | 输入内容与选择不变；同色 spinner；未就绪菜单显示可读说明 |
| error | label 正常 T，EI 标记 / E 边框 + 原选值；有焦点再叠 F | 不把轨道当错误色块；EI 标记与正常错误文本独立 | 输入 `T/C/E`；有焦点仍 F；菜单正文不整片变红 |

Checkbox、Radio、Switch 的状态值必须与真实模型同步；此矩阵仅规定视觉，不新增 fake checked 或延迟写操作。Select 的选中勾已经提供明确识别，青柠额外装饰标记不是必需；普通选项的正文与图标均中性。

### 3.5 卡片、普通标签与可选标签

| 状态 | 普通内容卡片 | 可交互/可选择卡片（仅已有此语义时） | 普通标签 / 来源标签 | 可选标签 |
|---|---|---|---|---|
| default | 标题 P、正文 T、辅助 S；`C/B` | 同普通卡片 | `S/C/B` | `P/C/B` |
| hover | ＝默认；非交互卡片不浮起 | `P/C/BH`，正文保持 T/S | N/A | `P/H/BH` |
| pressed | N/A | `P/C/BH`，不整卡填灰 | N/A | `P/A/BH` |
| selected | N/A | 保持 C、正常文字 + 局部 L 标记或 checked 控件 | N/A | `ON/L/ON` |
| focus | 容器 N/A；内部动作按自身状态 | 当前配色 `＋F` | N/A；关闭按钮按图标按钮 | 当前配色 `＋F` |
| readonly | 卡片内容本来可读，保持默认 | 去掉编辑入口但保留正常内容，静态卡片不假装 disabled | 保持正常文字 | 使用静态已选值，不渲染伪交互 |
| disabled | 整卡 N/A；只禁用内部不支持的操作 | 只禁用可选入口，关键内容与原因保留正常 T/S | 不因所属业务暂不可用而整体变灰 | `D/DB/B`，选择值保留 |
| loading | 保持 C/B；spinner、loading 说明为 P/S | 同左 | 显示正常文字的状态标签；不染成品牌色 | 原选值 `＋Busy` |
| error | C/B 不变；错误消息 T/S，EI 标记局部 | 原有选择和内容保留；E 信息独立 | 真实错误状态标签走语义体系 | 错误信息独立，不抹掉选择状态 |

以上普通卡片所有状态的阴影均为 none；只有真实浮层使用 OSh。标签“热门”“免费版”“本地合成”“已安装”应先判断其语义：来源、类别、安装值通常是正常可读的中性标签；确认的成功/风险状态才走独立语义色，不把所有“看起来积极”的标签刷成绿。

### 3.6 表格、弹层与弹层内操作

| 状态 | 表格表头 / 单元格 / 行 | 下拉、Popover、Modal、Drawer |
|---|---|---|
| default | 表头 S、正文 T；背景 C，细分隔 B；无默认灰斑马纹 | 标题 P、正文 T、辅助 S；背景 O、边框 B、投影 OSh |
| hover | 可操作行局部 H，前景不变；静态行可保持 C | 浮层壳不变；内部选项按 Select/菜单，按钮按按钮矩阵 |
| pressed | 仅可操作行局部 A；不改变行数据 | 浮层壳不变；内部动作按对应矩阵 |
| selected | 保持 C、正文 T；checkbox 或局部 L 标记表达，不整表绿底 | 浮层壳没有 selected；菜单选项按 Select，不改浮层背景 |
| focus | 当前行/单元格 `＋F`，或焦点停留在内部控件 | 实际焦点元素 `＋F`；壳体不额外叠一圈光晕 |
| readonly | 正常 T/C/B；可选择复制文本 | 正常内容；禁用写动作而不把全部说明变灰 |
| disabled | 容器 N/A；禁用单元格控件按其矩阵，业务数据仍正常文字 | 浮层壳 N/A；操作按钮独立 disabled，原因正常 T/S |
| loading | 保持表头、已有行与选值；同色 spinner + 明确 loading 说明 | O/B/OSh 保持；正常文字 busy，不用大面积灰层遮掉内容 |
| error | 保持表结构；错误说明 T/S、局部 EI 标记，可重试入口正常 | O/B/OSh 保持；EI 标记+正文，内部错误字段按输入规则 |

浮层“背景 O”与应用背景 N 是不同层级；暗色不可把所有内容压成同一黑色。浮层投影不等于输入焦点：内部输入获得焦点时可使用 F，但不能累加第二个模糊外阴影。Popover/Modal 出现与消失、焦点归还和键盘导航仍按既有交互，不在本轮改动。

## 4. 状态组合优先级

状态不是一张互斥色卡。按照下面顺序求值，并保留彼此独立的信息：

1. **能力与交互边界先确定**：hidden 不渲染；真正 disabled 阻止动作与 hover/pressed。只读只限制编辑，保留正常内容和可复制性。不要为颜色调整新增权限或持久化逻辑。
2. **确定真实 selected/checked 值**：只有真实当前路由、选择或已提交开关值才显示选中。loading/error 不清空选值，取消选择也不能由 hover 色暗示。
3. **在可操作控件上叠加 hover/pressed**：pressed 优先于 hover。已选导航 default 保持白/石墨原背景；hover/pressed 仅当前小项使用 H/A，L 标记始终保留。小筛选 selected 的 hover/pressed 使用 LH/LA。
4. **错误语义独立叠加**：错误边框 E 和消息保留正常正文。错误既不清除选中，也不覆盖全部标签/内容；危险动作使用已有独立语义。
5. **键盘 focus 始终可见**：对实际可聚焦元素叠 2 px F；selected+focus 保留 L 标记，error+focus 保留 E 边框及 F 外环。原生 disabled 不参与键盘 focus；若既有 aria-disabled 控件可聚焦以解释原因，仍保留 F 且不执行动作。
6. **loading 保持原角色并标记 busy**：spinner 使用当前可读前景；仅防重复提交不应把 pending 操作变成一片低对比灰。真正能力 disabled 与 busy 同时存在时，操作仍不可用，但原因与进度文本不使用 D。

### 必须一起查看的组合

| 组合 | 必须保留的结果 | 禁止结果 |
|---|---|---|
| selected + focus | 原选中值/3 px L + 2 px F | 焦点把选中清掉，或选中覆盖焦点 |
| error + focus | E 字段边界 + F 焦点 + 正常错误说明 | 只剩红框看不出焦点，或 focus 让错误消失 |
| readonly + focus | 正常可读值 + F，可复制 | 与 disabled 同灰、不能选择文字 |
| disabled + hover | 禁用外观不变，动作不执行 | 仍出现 hover/pressed 反馈并看似可用 |
| loading + focus | 当前动作/选值 + spinner + F | 整体 opacity 导致正文和进度不可读 |
| selected + loading/error | 选择值持续可见，busy/error 另外表达 | 重新闪回 default 或误报操作已成功 |

## 5. 青柠、浅绿、深绿的允许清单

| 颜色角色 | 允许 | 不允许 |
|---|---|---|
| 品牌青柠 L/LH/LA | 已批准 Logo 折角；主按钮；小型筛选/标签选中；checkbox/radio/switch 选中；导航 3 px 标记；原生文本选区底色 | 白底正文、普通线性图标、权限说明、焦点环、整页/整卡背景 |
| 旧浅绿 / 深绿 soft | 不用于普通 UI；旧 token 只作为兼容槽位并映射到中性表面 | 普通导航选中底、hover、标签、图标底板、消息/附件默认底 |
| 深绿 `#4B651D` | 图表专用首序列，保留原信息可读性 | brand-text、普通正文、普通操作图标、焦点 |
| 真正 success 语义绿 | 已确认的成功、完成、健康状态，文字/图标使用 SI；原状态主色不变 | 普通按钮、所有“积极”标签、把“本地合成”冒充成功状态 |
| 非品牌状态/图表色 | 现有 error/warning/info/Agent 状态、独立图表系列 | 因统一配色把它们批量替换成青柠或灰色 |

**图表隔离特别注意**：`chart-series-1` 独立固定为亮色 `#4B651D`、暗色 `#C3F35B`，不得再次指向已经中性化的 brand-text。其他七个系列和语义状态规则继续保留。

## 6. 可读性与实际使用规范

- P/T/S/M 必须按内容用途分配。正文、说明、元信息有层级，但都承担有效信息；D 只给真正 disabled 控件。权限失败、加载失败和只读说明必须使用可读的 T/S。
- 在亮色白底上 P/T 对比约 14.82:1、S 约 7.4:1、M 约 4.65:1；ON 对 L 约 11.50:1。具体数值按实际组件检查计算，透明度或叠层后的结果另验，不能用不透明 token 的比值代替实际组件。
- 普通文字目标至少 4.5:1；键盘焦点与必要控件轮廓至少 3:1。暗色默认 BC 对 O 表面约 2.65:1，因此暗色 Modal/Popover/Drawer 内 Input 与 Select 的默认轮廓提升为 BH `#969EA8`（统一浮层适配），不可把 BC 对 C 的结果直接当作对 O 也通过。装饰卡片边框 B 不独立承载“可点击”含义；输入和选择控件用 BC。无障碍不靠全局把所有细边框加黑实现。
- active 不是文字变色的借口：青柠按钮始终 ON，白底次按钮始终 P。普通图标继承当前中性前景；Logo 与图表不被全局 `svg { color: ... }` 误伤。
- 普通 NInput 与 NSpin 的 loading 图标使用 P；带 error/warning 状态的输入 spinner 分别使用 EI/WI。主青柠按钮内部 spinner 保持 ON，不因统一普通 loading 图标而变为浅色。
- focus 只改变焦点表达，不改变文字/布局。2 px 中性无 glow 的标准覆盖 Composer、Naive 输入、选项与图标按钮；不能把每个元素都加 4 px 品牌描边。
- Light 结构全白；H/A/DB 只能是局部状态。反例：`Page background = control-hover`、`Card background = disabled-bg`、`Sidebar background = brand-soft`。
- 正常卡片、输入、普通图标底板为无阴影；浮层用 OSh。反例：卡片一 hover 就用大阴影，输入 focus 叠卡片阴影与绿色 glow。
- 样张应使用真实 NButton、NInput、NMenu、NCheckbox、NRadio、NSwitch、NSelect 与现有 Yj 组件检查继承；不能只画色块宣称所有组件已经适配。

本矩阵已生效。Chat、我的店铺、插件是本次审核的三页重点；全部页面后续按同一角色与状态规则使用。验证记录必须标明实际环境、窗口和状态，浏览器组件验证不得称为 Tauri / WebKit 或原生可访问性验收。规范生效与某项运行验证通过是两个不同事实；具体结果以本次验证记录为准。


## 7. 已有业务组件的颜色归位

- Chat 普通消息、附件和文件图标使用中性 card/边框/正文；composer 使用同一 2px 中性焦点，文本选区与焦点并存。
- 审批 pending/cancel/disconnected 保留 warning，accepted 保留 success，error 保留 error；submitting/reconciling 与对话 progress 使用独立 info，不借用品牌青柠表达执行阶段。状态字段与交互不改变。
- 真实语义状态的小字、Alert 标题/图标与指标趋势使用对应 semantic ink；语义底色和边框保留原状态含义。
- 工作流普通类别图标/节点不使用图表或品牌 accent 色；真实图表自身的系列色不变。促销/来源标签使用中性角色，当前只读视图保留真实 selected 表达。
- 已禁用的推荐演示/执行按 disabled 优先级使用中性不可用状态；这只改变颜色，不能新增点击或业务效果。
