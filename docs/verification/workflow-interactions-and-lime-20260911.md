# 工作流页面交互与青柠点缀验收

日期：2026-09-11。

`contract-impact = none`：仅修改 Desktop 展示文案、组件内存选择与样式；Desktop 跨进程交互、API / Agent Host 协议、本地持久状态的格式和值均无可观察变化。没有改动原生配置、权限、依赖或二进制。

## 实现范围

- 使用用户指定的新说明，“创建工作流”采用青柠文字与石墨小底板，保持亮暗主题可读。
- 顶部分类、我的分类、网格/列表可独立选中；排序有最近修改、最近创建、名称排序三个选项。
- 创建、查看全部、每张卡片的更多按钮提供可取消的选中反馈；推荐卡内演示/执行互斥选中，再次点击取消。
- 所有交互保持八张卡片的内容、顺序、布局不变，离开页面后选择重置，不调用创建或执行能力。
- 八张卡片使用青柠悬停/焦点描边与 2px 上移；图标底板、推荐标签、处理中间节点补充青柠点缀。
- 原生 button/select 提供键盘操作与选中语义，焦点轮廓为 2px 中性描边。

## 自动检查

- `pnpm lint`：通过，包含 ESLint 与 Vue/TypeScript 类型检查。
- `pnpm exec vitest run src/pages/workflows/WorkflowPage.test.ts src/domain/workflow-showcase.test.ts src/authorization/workflow-showcase-ui-config.test.ts --maxWorkers=4 --exclude '**/.local/**' --exclude '**/.contracts-source/**' --exclude '**/.agent-host-source/**' --exclude '**/.skills-source/**'`：3 个文件、9 项检查通过。
- 回归覆盖完整文案、所有分类/视图选项、三个排序值、独立按钮选中/取消、卡片文本和顺序不变、重新挂载时重置、无 API/原生/持久化依赖、axe 无 serious/critical 违规。
- `make build`：通过；Vite 提示已有的大于 500kB chunk，不影响构建成功。
- `pnpm docs:build`：通过，包括品牌资产检查、参考代码类型检查、VitePress 构建。
- 首次定向 Vitest 命令未排除 `.local`，额外发现历史源码副本，8 项历史副本测试因重复 Vue runtime 失败；按仓库标准增加副本目录排除后，当前源码的 9 项检查全部通过。没有更改测试期望来掩盖该环境问题。

## 实际浏览器验证

环境：Codex 内置浏览器，使用 `tests/visual/feat-151` 加载真实页面组件。结果不等同于易界 Tauri 应用验收。

| 视口 | 主题 | 卡片列数 | 页面/主体横向溢出 | axe |
| --- | --- | --- | --- | --- |
| 1180×760 | light | 2 | 无 | 0 violations |
| 1180×760 | dark | 2 | 无 | 0 violations |
| 1440×900 | light | 4，每列 276px | 无，卡内也无横向溢出 | 0 violations |
| 1440×900 | dark | 4，每列 276px | 无 | 0 violations |

鼠标实测八张卡片均进入 `:hover`，最终边框为既有青柠 token（rgb(195, 243, 91)），transform 为 `matrix(1, 0, 0, 1, 0, -2)`。实测分类、图片处理、列表视图、最近创建/名称排序切换后，八张卡片的完整文本仍与初始一致；推荐卡从演示切到执行时，前者取消、后者选中。

键盘 Enter 可选中创建和推荐操作；Space 可选中列表视图，并保持可见 2px 焦点轮廓。青柠说明文字的底色为 rgb(37, 40, 43)。亮暗主题的顶部、推荐区域、四列布局截图保存在本地 `.local/workflow-ui-20260911/`。

## 未执行项目、原因及影响

- 未运行完整 `make lint` / `make test`：共用的 `generate:check` 中 `scripts/check-agent-host-v3-contract.mjs` 会验证 `injection-invalid.json` 攻击注入 fixture，Rust 全量测试还包含错误权限/链接故障场景。遵循用户禁止攻击注入与权限故障测试的硬性规则，改为正常前端 lint、工作流定向检查、构建及真实 UI 操作。本次不能声称全仓契约和 Rust 全量回归通过。
- 首轮未进行易界 Tauri 应用启动、打包或原生验收：当时仅修改前端组件，浏览器验证不能替代应用验收；用户随后授权重启后的结果见下文。
- 未改变系统“减少动态效果”设置：已检查对应 CSS 分支会取消过渡和位移、保留描边；未将该分支记为实际系统设置验收通过。

## 用户授权重启后的原生复验

用户反馈页面未更新并授权启动后，确认当时运行的是 23:25 启动的本地 debug 应用包，加载 `tauri://localhost/workflows` 的内嵌旧资源。前端 `make build` 本身不会替换该应用包内的资源。

通过应用自身 Command-Q 正常退出；确认 Desktop、Agent Host、Codex 子进程全部退出，18081 端口释放后，运行标准 `pnpm tauri:demo-fast:app`。标准流程完成前端与 Rust debug 构建、未签名的本地应用打包和启动；未修改受审计 Codex Runtime，未使用强杀或故障注入。

已在新启动的易界应用中打开工作流页，实际看到新文案、青柠点缀和可切换按钮；点击“电商获客”显示选中，原生排序菜单显示“最近修改 / 最近创建 / 名称排序”，选择“最近创建”后控件值更新。应用保持运行并停留在工作流页面。此补充覆盖原生启动与上述基础交互，不扩展为完整原生回归结论。

## 2026-09-12 按用户要求回滚后续结构升级

用户明确要求撤销后续的两列方案卡/石墨图标底板升级，已恢复本记录对应的 1.2.0 界面：两组卡片采用相同 2/4 列响应式布局、青柠图标底板与推荐标签、原有节点图标和排版。新文案、按钮选中、三个排序选项、卡片悬停及键盘焦点仍保留。被撤销方案的源码和验收记录仅归档到 `.local/workflow-redesign-20260912/`，不作为当前规范。

`contract-impact = none`：回滚仅恢复本地展示结构与常量，Desktop 跨进程交互、API / Agent Host 协议、本地持久状态均无可观察变化。

回滚后 `pnpm lint`、3 个文件的 9 项定向 Vitest 检查、`pnpm docs:build` 均通过。经正常退出再运行 `pnpm tauri:demo-fast:app`，前端/Rust debug 构建及应用打包成功；生成的工作流资源名称恢复为上一成功交付中的 `WorkflowPage-BuGaEjXg.css` 与 `WorkflowPage-Dm6XzQ5T.js`。已在重新启动的易界应用打开工作流页，确认青柠底板和原有卡片结构恢复，应用保持运行。前述未执行的安全测试范围不变。

## 2026-09-12 标签、操作层级与更多图标收敛

按用户追加的三点要求，热门/推荐标签恢复原有中性配色；执行为默认青柠填充、石墨半粗文字的主按钮，演示为中性次按钮，选中演示时也不染青柠。四张“我的工作流”卡片右上角更多恢复为具备可访问名称的展示图标，移除选中状态、点击处理、按钮悬停样式和 Tab 停靠点。现有卡片布局、尺寸、图标底板、流程节点、排序/分类/视图交互保持不变。

`contract-impact = none`：仅修改本地展示语义与样式，Desktop 跨进程交互、API / Agent Host 协议、本地持久状态均无可观察变化。

`pnpm lint`、3 个文件的 9 项定向 Vitest、`pnpm docs:build` 通过。浏览器实测 light/dark 的 1180×760 与 1440×900 均无主体横向溢出，axe 为 0 violations。点击“更多”前后的元素内容、背景与边框完全相同，且不获得焦点；演示选中后背景分别为亮色中性灰 rgb(236,238,240) 和暗色中性灰 rgb(65,72,80)，字重 400，执行仍为青柠 rgb(195,243,91)、字重 600。四列布局保持每列 276px。截图在 `.local/workflow-actions-20260912/`。

经应用自身正常退出、确认子进程和端口释放后，标准 `pnpm tauri:demo-fast:app` 完成前端、Rust debug 构建与本地应用打包并启动。原生窗口截图确认灰色热门/推荐标签、青柠执行按钮、原有四列布局均已生效；更多项在原生可访问树中为 image，不再为 toggle button。应用保持运行。全仓故障/攻击注入测试的跳过范围和原因沿用前文，未重新执行。

## 2026-09-12 标题图标与推荐卡精简

“我的工作流”新增 Workflow 图标，“推荐工作流”由 RefreshCw 改为 Sparkles；两处均通过 YjSection 使用 20px 中性图标，间距与对齐一致。所有推荐卡的输入/处理/输出三步示意（编号、图标、名称、连接箭头）及对应 CSS 已移除，卡片保留标题与灰色标签、说明、底部使用量和演示/执行按钮。最小高度收紧到 192px，标题自然均衡换行、同排底部操作对齐，保留原有 2/4 列响应式栅格及交互。

`contract-impact = none`：仅改动本地标题图标与展示布局；Desktop 跨进程交互、API / Agent Host 协议、本地持久状态均无可观察变化。

`pnpm lint`、3 个文件的 9 项定向 Vitest、`pnpm docs:build` 通过。浏览器检查 light/dark 的 1180×760 与 1440×900 均无主体横向溢出，axe 为 0 violations；流程示意元素为 0。1440px 窗口实测四张推荐卡等高 193px，页脚均位于 y=730；1180px 窗口推荐卡等高 192px。键盘执行按钮的选中与焦点正常。截图在 `.local/workflow-compact-20260912/`。

通过应用自身正常退出后，标准 `pnpm tauri:demo-fast:app` 已完成前端、Rust debug 构建与本地打包并重新启动。原生页面截图确认两个标题图标、四列紧凑推荐卡、灰色标签和青柠执行按钮已生效，原生可访问树中不再有三步流程标注。应用保持运行。未执行的故障/攻击注入测试范围沿用前文。

## 2026-09-12 去除图标底色与替换推荐图标

按用户最新要求，四张“我的工作流”卡片图标背景改为透明，前景随主题使用主文字色；推荐标题由 Sparkles 改为现有 registry 中的 Route 路径节点图标。图标容器仍为 40px，其他尺寸、布局、标签、选中和执行按钮样式不变。`contract-impact = none`：纯展示变更，不涉及跨进程交互、API / Agent Host 协议或本地持久状态。

`pnpm lint`、3 个文件的 9 项定向 Vitest、`pnpm docs:build` 均通过。浏览器 1180×760 亮暗主题实测：四个图标背景均为 rgba(0,0,0,0)，前景分别为 rgb(37,40,43) 和 rgb(245,247,250)，推荐标题实际渲染 lucide-route，主体无横向溢出，axe 为 0 violations。

经正常退出并运行 `pnpm tauri:demo-fast:app`，前端、Rust debug 构建及本地应用打包成功。重新打开工作流页，原生截图确认四张卡片已去掉青柠图标底板。应用保持运行；故障/攻击注入测试的跳过范围仍沿用前文。

## 2026-09-12 分类自然换行与说明文字去底板

移除顶部分类在 980px 以下固定五列、600px 以下固定三列的覆盖，统一按按钮自身宽度 flex 自然换行。说明中的“创建工作流”按用户要求移除石墨背景、圆角和内边距，保留青柠字色。`contract-impact = none`：仅调整 CSS，Desktop 跨进程交互、API / Agent Host 协议和本地持久状态均无可观察变化。

`pnpm lint`、3 个文件的 9 项定向 Vitest、`pnpm docs:build` 均通过。浏览器实测 1180×760 亮暗主题：分类可用宽度为 892px，首行八项、次行一项，首行右侧仅余 16px；1280×800 亮色主题的可用宽度为 992px，九项全部同一行。均无主体横向溢出，文字背景为 rgba(0,0,0,0)。截图保存在 `.local/workflow-categories-20260912/`。

无障碍实际结果：暗色主题 axe 为 0 violations；亮色主题因用户指定保留白底青柠文字，`.workflow-page__create-highlight` 检出 1 项 serious color-contrast，实测 1.28:1，低于 4.5:1。保留本次明确要求的配色，不屏蔽检查，不将亮色 axe 记为通过。前述故障/攻击注入测试仍未执行，原因及影响不变。

通过应用自身 Command-Q 正常退出，确认 Desktop、Agent Host、Codex 子进程退出及 18081 端口释放后，运行标准 `pnpm tauri:demo-fast:app`。前端、Rust debug 构建及未签名本地应用打包成功；重新启动并进入工作流页，原生截图确认分类首行充分利用右侧空间，说明文字黑底已移除。应用保持运行。
